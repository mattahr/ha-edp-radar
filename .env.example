# Copy to .env to override the host ports used by `dev/ha.sh` / F5.
# 8123/5678 are left free for other Home Assistant dev containers (D19).
# If you change DEBUGPY_PORT, also change the port in .vscode/launch.json.
HA_PORT=8125
DEBUGPY_PORT=5679
