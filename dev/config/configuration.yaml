# Minimal Home Assistant configuration for developing the integration.
default_config:

# Lets VS Code attach on host port 5679 (container port 5678, see .vscode/launch.json).
debugpy:
  start: true
  wait: false

logger:
  default: warning
  logs:
    custom_components.edp_radar: debug
